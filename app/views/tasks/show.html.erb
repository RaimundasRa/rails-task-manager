<h1><%= @task.title %></h1>
<br>
<p><%= @task.details %></p>
<br>

<%= link_to "Go back to tasks", tasks_path %>

<%= link_to "Edit task", edit_task_path(@task) %>

<%= link_to "Delete task", task_path(@task), method: :delete, data: { confirm: 'You sure you want to delete?' } %>
